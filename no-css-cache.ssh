timestamp=$(date +%s)
VERSION_NO_CACHE=timestamp
echo VERSION_NO_CACHE=timestamp

while IFS= read -r line
do
    case "$line" in
      *VERSION_NO_CACHE*) echo "${line/VERSION_NO_CACHE/$VERSION_NO_CACHE}" >> dialogflow/agent.json ;;
      *) echo "$line" >> dialogflow/agent.json ;;
    esac
done < dialogflow/agent-old.json
echo "}" >> dialogflow/agent.json