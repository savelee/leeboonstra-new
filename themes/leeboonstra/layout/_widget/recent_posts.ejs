<% if (site.posts.length){ %>
    <h4>Recent Conversational Articles</h4>
    <ul class="sidebar-module-list">
      <% site.posts.sort('date', -1).limit(8).each(function(post){ %>
        <li>
          <a href="<%- url_for(post.path) %>"><%= post.title || '(no title)' %></a>
        </li>
      <% }) %>
    </ul>
<% } %>